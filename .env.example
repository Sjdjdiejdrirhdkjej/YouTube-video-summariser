# Algolia (for Search.tsx - optional)
VITE_ALGOLIA_APP_ID=your_app_id
VITE_ALGOLIA_SEARCH_API_KEY=your_search_key

# Video summarizer backend (required for /api/summarize)
# Put this in a file named .env in the project root (same folder as package.json).
# Use: GEMINI_API_KEY=your_actual_key with no quotes and no spaces around =
GEMINI_API_KEY=your_gemini_api_key
